services:
  # Layer 1: 공급망 데이터 백본 (PostgreSQL + PostGIS + pgvector)
  db:
    build:
      context: .
      dockerfile: docker/db.Dockerfile
    container_name: kira-postgres
    networks:
      - project_net
    env_file:
      - ./.env
    # ports 제거: 외부 노출 안 함. 컨테이너 내부 통신(app→db:5432)만 사용.
    # EC2에서 직접 DB 보려면 SSH 터널 사용.
    volumes:
      - kira_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-kira_admin} -d $${POSTGRES_DB:-kira_db}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always

  # Async Workflow Queue용 Redis (ARQ)
  redis:
    build:
      context: .
      dockerfile: docker/redis.Dockerfile
    container_name: kira-redis
    networks:
      - project_net
    # ports 제거: 외부 노출 안 함. 내부 통신만.
    volumes:
      - kira_redisdata:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep PONG || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: always

  # Layer 2: KIRA 에이전트 통합 백엔드 (FastAPI)
  app:
    build:
      context: .
      dockerfile: docker/app.Dockerfile
    container_name: kira-backend
    networks:
      - project_net
    env_file:
      - ./.env
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    # ports 제거: 외부 노출 안 함. nginx가 내부에서 app:8000으로 전달.
    # volumes 제거: 이미지 안의 코드를 그대로 사용 (CI/CD 대비, 운영 정석).
    expose:
      - "8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always

  # Layer 2: ARQ 워커 (Queue 컨슈머) — app과 동일 이미지, 실행 커맨드만 다름
  worker:
    build:
      context: .
      dockerfile: docker/app.Dockerfile
    container_name: kira-worker
    command: ["arq", "backend.workers.geo_risk_worker.WorkerSettings"]
    networks:
      - project_net
    env_file:
      - ./.env
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    # volumes 제거: app과 동일 이유.
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always

  # Layer 0: Nginx 리버스 프록시 (정문 — gzip 압축 + app 전달)
  nginx:
    image: nginx:1.27-alpine
    container_name: kira-nginx
    networks:
      - project_net
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    restart: always

volumes:
  kira_pgdata:
    name: kira_pgdata
  kira_redisdata:
    name: kira_redisdata

networks:
  project_net:
    driver: bridge
